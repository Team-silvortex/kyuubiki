import test from "node:test";
import assert from "node:assert/strict";
import fs from "node:fs";
import path from "node:path";
import { fileURLToPath } from "node:url";

const ROOT = path.resolve(path.dirname(fileURLToPath(import.meta.url)), "..");

function read(relativePath) {
  return fs.readFileSync(path.join(ROOT, relativePath), "utf8");
}

test("hub shell exposes the desktop information architecture", () => {
  const html = read("ui/index.html");

  assert.match(html, /data-target="projects"/);
  assert.match(html, /data-target="runtimes"/);
  assert.match(html, /data-target="deploy"/);
  assert.match(html, /data-target="observe"/);
  assert.match(html, /data-target="tools"/);
  assert.match(html, /Open workbench/);
  assert.match(html, /data-action="open-installer"/);
  assert.match(html, /Start local stack/);
  assert.match(html, /Stage desktop package/);
  assert.match(html, /Build host bundles/);
  assert.match(html, /Verify desktop release/);
  assert.match(html, /hub-action-state/);
  assert.match(html, /assistant-engine-state/);
  assert.match(html, /assistant-mode-local/);
  assert.match(html, /assistant-mode-llm/);
  assert.match(html, /assistant-local-cards/);
  assert.match(html, /assistant-llm-panel/);
  assert.match(html, /assistant-base-url/);
  assert.match(html, /assistant-api-key/);
  assert.match(html, /assistant-model-preset/);
  assert.match(html, /assistant-model-name/);
  assert.match(html, /assistant-endpoint-policy/);
  assert.match(html, /assistant-request-plan/);
  assert.match(html, /assistant-execute-plan/);
  assert.match(html, /assistant-plan-actions/);
  assert.match(html, /assistant-output/);
  assert.match(html, /assistant-audit-list/);
  assert.match(html, /project-bundle-path/);
  assert.match(html, /project-bundle-compare-path/);
  assert.match(html, /project-bundle-out-path/);
  assert.match(html, /recent-bundle-list/);
  assert.match(html, /recent-compare-list/);
  assert.match(html, /recent-output-list/);
  assert.match(html, /favorite-action-list/);
  assert.match(html, /recent-action-list/);
  assert.match(html, /data-history-filter="all"/);
  assert.match(html, /data-history-filter="failed"/);
  assert.match(html, /data-history-filter="inspect"/);
  assert.match(html, /data-history-filter="normalize"/);
  assert.match(html, /data-history-filter="diff"/);
  assert.match(html, /data-history-manage="keep-failed"/);
  assert.match(html, /data-history-manage="import-json"/);
  assert.match(html, /data-history-manage="export-json"/);
  assert.match(html, /data-history-manage="clear"/);
  assert.match(html, /history-import-input/);
  assert.match(html, /Generate plan/);
  assert.match(html, /Inspect \.kyuubiki/);
  assert.match(html, /Validate \.kyuubiki/);
  assert.match(html, /Normalize bundle/);
  assert.match(html, /Unpack bundle/);
  assert.match(html, /Pack project/);
  assert.match(html, /Diff bundles/);
});

test("hub shell registers section switching behavior", () => {
  const js = read("ui/app.js");
  const bridge = read("ui/shared/tauri-bridge.js");

  assert.match(js, /sectionModel/);
  assert.match(js, /setSection/);
  assert.match(js, /runAction/);
  assert.match(js, /setBusy/);
  assert.match(js, /setAssistantOutput/);
  assert.match(js, /setProjectBundleOutput/);
  assert.match(js, /currentAssistantSnapshot/);
  assert.match(js, /renderAssistantContext/);
  assert.match(js, /setAssistantMode/);
  assert.match(js, /buildHubAssistantLocalCards/);
  assert.match(js, /renderHubAssistantLocalCards/);
  assert.match(js, /requestHubAssistantPlan/);
  assert.match(js, /validateAssistantBaseUrl/);
  assert.match(js, /updateAssistantEndpointPolicy/);
  assert.match(js, /renderHubAssistantPlan/);
  assert.match(js, /executeHubAssistantAction/);
  assert.match(js, /executeHubAssistantPlan/);
  assert.match(js, /loadHubAssistantAudit/);
  assert.match(js, /persistHubAssistantAudit/);
  assert.match(js, /rememberHubAssistantAudit/);
  assert.match(js, /renderHubAssistantAudit/);
  assert.match(js, /confirmHubAssistantAction/);
  assert.match(js, /assistantRiskLevel/);
  assert.match(js, /mirrorHubAssistantAuditToSecurityEvents/);
  assert.match(js, /updateHubAssistantAuditDelivery/);
  assert.match(js, /currentOrchestratorBaseUrl/);
  assert.match(js, /currentAssistantAuditContext/);
  assert.match(js, /assistantDeliveryStateClass/);
  assert.match(js, /loadHubRecents/);
  assert.match(js, /loadHubAssistantSettings/);
  assert.match(js, /persistHubAssistantSettings/);
  assert.match(js, /loadHubAssistantSecrets/);
  assert.match(js, /persistHubAssistantSecrets/);
  assert.match(js, /saveProjectBundleRecents/);
  assert.match(js, /rememberProjectBundleAction/);
  assert.match(js, /summarizeProjectActionResult/);
  assert.match(js, /formatProjectActionTime/);
  assert.match(js, /projectActionStateClass/);
  assert.match(js, /normalizeImportedProjectAction/);
  assert.match(js, /mergeProjectActionHistory/);
  assert.match(js, /actionIdentity/);
  assert.match(js, /shellQuote/);
  assert.match(js, /buildProjectCliCommand/);
  assert.match(js, /buildPythonMacroStub/);
  assert.match(js, /copyProjectCliCommand/);
  assert.match(js, /copyPythonMacroStub/);
  assert.match(js, /sortProjectActionHistory/);
  assert.match(js, /renamePinnedProjectAction/);
  assert.match(js, /renderHubRecents/);
  assert.match(js, /renderRecentActionHistory/);
  assert.match(js, /renderEmptyHistoryState/);
  assert.match(js, /renderProjectActionEntries/);
  assert.match(js, /renderHistoryFilters/);
  assert.match(js, /matchesHistoryFilter/);
  assert.match(js, /currentFilteredHistoryActions/);
  assert.match(js, /downloadHubJson/);
  assert.match(js, /exportRecentActionHistory/);
  assert.match(js, /importRecentActionHistory/);
  assert.match(js, /togglePinnedProjectAction/);
  assert.match(js, /manageRecentActionHistory/);
  assert.match(js, /restoreProjectActionContext/);
  assert.match(js, /rerunProjectActionEntry/);
  assert.match(js, /runProjectBundleAction/);
  assert.match(js, /Pin/);
  assert.match(js, /Pinned/);
  assert.match(js, /Label/);
  assert.match(js, /Copy CLI/);
  assert.match(js, /Copy Python/);
  assert.match(js, /Re-run/);
  assert.match(js, /Restore/);
  assert.match(js, /Run action/);
  assert.match(js, /restored .* context/);
  assert.match(js, /status: "ok"/);
  assert.match(js, /status: "failed"/);
  assert.match(js, /pinned: Boolean/);
  assert.match(js, /favoriteLabel/);
  assert.match(js, /filtered recent actions:/);
  assert.match(js, /unpinned/);
  assert.match(js, /entry\.pinned \? "unpinned" : "pinned"/);
  assert.match(js, /No pinned favorites yet\./);
  assert.match(js, /No non-pinned actions in this view\./);
  assert.match(js, /kept failed recent actions only/);
  assert.match(js, /imported .* recent actions from JSON/);
  assert.match(js, /failed to import recent action history:/);
  assert.match(js, /exported .* recent actions as JSON/);
  assert.match(js, /copied CLI command for/);
  assert.match(js, /copied Python stub for/);
  assert.match(js, /assistant request failed/);
  assert.match(js, /localhost \/ 127\.0\.0\.1/);
  assert.match(js, /API key is sent directly to the configured base URL/);
  assert.match(js, /Sensitive/);
  assert.match(js, /High-risk/);
  assert.match(js, /assistant action/);
  assert.match(js, /model plan action/);
  assert.match(js, /No assistant actions recorded in this session\./);
  assert.match(js, /hub\.assistant\.action/);
  assert.match(js, /control-plane sync failed/);
  assert.match(js, /sync_failed/);
  assert.match(js, /synced/);
  assert.match(js, /Executed .* assistant actions\./);
  assert.match(js, /updated label for/);
  assert.match(js, /cleared recent action history/);
  assert.match(js, /currentProjectBundleOutputPayload/);
  assert.match(js, /currentProjectBundleComparePayload/);
  assert.match(js, /project_bundle_inspect/);
  assert.match(js, /project_bundle_validate/);
  assert.match(js, /project_bundle_normalize/);
  assert.match(js, /project_bundle_unpack/);
  assert.match(js, /project_bundle_pack/);
  assert.match(js, /project_bundle_diff/);
  assert.match(js, /launch_workbench_gui/);
  assert.match(js, /launch_installer_gui/);
  assert.match(js, /data-target-section/);
  assert.match(js, /desktop_build_host/);
  assert.match(js, /desktop_stage/);
  assert.match(js, /desktop_verify/);
  assert.match(js, /hub-nav__item--active/);
  assert.match(js, /addEventListener\("click"/);
  assert.match(bridge, /invokeTauri/);
  assert.match(bridge, /loadDesktopBrand/);
});

test("tauri backend exposes hub runtime commands", () => {
  const rust = read("src-tauri/src/main.rs");

  assert.match(rust, /service_status/);
  assert.match(rust, /service_start/);
  assert.match(rust, /service_restart/);
  assert.match(rust, /service_stop/);
  assert.match(rust, /read_runtime_log/);
  assert.match(rust, /doctor_report/);
  assert.match(rust, /validate_env/);
  assert.match(rust, /desktop_stage/);
  assert.match(rust, /desktop_verify/);
  assert.match(rust, /desktop_build_host/);
  assert.match(rust, /project_bundle_inspect/);
  assert.match(rust, /project_bundle_validate/);
  assert.match(rust, /project_bundle_normalize/);
  assert.match(rust, /project_bundle_unpack/);
  assert.match(rust, /project_bundle_pack/);
  assert.match(rust, /project_bundle_diff/);
  assert.match(rust, /run_project_cli/);
  assert.match(rust, /run_project_cli_with_output/);
  assert.match(rust, /run_project_cli_compare/);
  assert.match(rust, /launch_desktop_app_with_fallback/);
  assert.match(rust, /launch_built_desktop_app/);
  assert.match(rust, /launch_workbench_gui/);
  assert.match(rust, /launch_installer_gui/);
  assert.match(rust, /hub_environment/);
});
