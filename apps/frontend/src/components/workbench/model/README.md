# Model Surfaces

This directory contains modeling-side workbench surfaces such as tool cards,
material cards, parametric generators, and model-tree panels.

Keep pure editing commands and stable view-model mappers in `src/lib/workbench`
when they no longer need to live inside JSX.
